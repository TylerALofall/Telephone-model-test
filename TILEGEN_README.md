# Orange tile generator

Build:

```bash
cc -std=c99 -O2 -Wall tilegen.c -lm -o tilegen
```

Run:

```bash
./tilegen
```

Output:
- 59 SVG files under `out/03_colors/orange/`
- Optional bundle: `out.zip` (create with `zip -r out.zip out`)
